# LLM 缓存的破坏模式

> Prompt Caching 的生效条件极苛刻：输入前缀必须稳定。任何打破前缀稳定性的行为——换模型、换工具、换用户、换项目——都会让缓存瞬间失效。

---

## 一、核心原理

Prompt Caching 的本质是 **KV Cache 复用**：服务器把之前计算过的 Key-Value 激活状态存在显存中，下次遇到相同前缀时跳过计算。

生效条件：
- **前缀必须 100% 一致**——哪怕一个字符不同，哈希就对不上
- **缓存有 TTL**——通常 5-30 分钟，过期即清除
- **缓存有容量限制**——LRU 淘汰，新请求会挤掉旧缓存

这三个限制决定了：**任何导致前缀变化、显存竞争、或时间间隔过长的操作，都是缓存的杀手。**

---

## 二、IDE 场景：持续变化的前缀

IDE 插件（Copilot、Cursor、Cline）是缓存的"死敌"——不是因为工程做得差，是因为架构层面互斥。

**IDE 的工作循环**：用户打字 → 上下文变化 → 触发请求。每一次按键都在改变前缀。

IDE 会在后台拼凑：
- 光标前后几百行代码
- 打开的所有标签页
- 终端日志
- RAG 检索的相关片段

用户每打一个字，前缀都在变，缓存连一秒钟都维持不住。

更根本的是：**IDE 的核心 KPI 是准确率，不是省钱。**"宁可多传，不可漏掉"——RAG、AST 分析把每次不同的代码片段塞进 Prompt，与"前缀一致"的缓存机制完全背道而驰。

→ IDE 的务实妥协：高频 Tab 补全交给本地 3B-7B 小模型，复杂任务才走云端。

---

## 三、多模型切换：物理隔离

在不同模型之间切换（如 Hermes ↔ Codex），缓存**100% 失效**。三层原因：

### 3.1 模型物理隔离

KV Cache 是存在显卡显存中的激活状态，与特定模型实例强绑定。Hermes 的缓存，Codex 绝对无法读取——即使它们基于相同的基座（如 Llama）。

### 3.2 LRU 淘汰

切换期间，服务器为了腾出显存处理其他请求，会把你之前的缓存清除。等你切回来，缓存大概率已经不在了。

### 3.3 Prompt Template 冲突

不同模型的特殊 Token 和输入格式完全不同（`<|im_start|>user` vs `### Instruction:`）。即使用户文本一样，隐藏的格式标记不同，二进制哈希对不上，缓存报废。

---

## 四、多工具混用：前缀位置决定缓存命运

工具本身不必然破坏缓存。关键变量是**工具结果在 prompt 中的位置**。

### 4.1 结果追加在末尾 → 缓存保留

```
[system] [历史对话] [工具结果_A]
                   [工具结果_B]    ← B 追加在 A 后面
```

每次工具调用的结果追加在 prompt 末尾，前缀 `[system] [历史对话] [工具结果_A]` 不变，后续请求仍可命中缓存。这是 Agno、LangChain 等框架的默认行为——工具结果 append 到消息列表末尾。

### 4.2 结果插入中间 → 缓存破坏

```
[system] [历史对话]          ← 原始前缀
        [工具结果_A]          ← 插入位置1
        [工具结果_B]          ← 插入位置2（和 A 不同位置）
```

如果框架把工具结果插入到 prompt 的不同位置（如 RAG 片段插到 system prompt 之后、用户消息之前），每次插入位置不同，前缀断裂，缓存全部失效。

### 4.3 System Prompt 切换 → 缓存破坏

不同工具自带专属 System Prompt（画图工具注入图片生成指令，数据分析注入 Python 规范）。切换工具 = 切换 system prompt = 前缀第一个 token 就不同 = 缓存从头失效。

### 4.4 大体积结果 → LRU 挤出

工具 A 生成 20KB 文本，工具 B 紧接着抓取 50KB 网页——大体积输入占用显存，服务器为腾出空间处理新数据，可能挤掉之前的缓存。这是 LRU 淘汰，不是前缀问题。

---

## 五、路由聚合：缓存击穿陷阱

使用第三方路由服务（OpenRouter 等）时，缓存命中率可能远低于直接调用官方 API。

### 5.1 成本倒挂

缓存命中 = 标准输入价的 10-20%。缓存未命中 = 100% 标准价。

路由服务为了提高底层账号利用率，将请求随机分发给不同账号——每个账号的上下文被其他用户覆盖，缓存命中率从 95% 暴跌至 20%。结果：基础单价打 8 折，总账单飙升 2-3 倍。

### 5.2 路由服务的自救

- **粘性会话路由**（Sticky Session）：同一个对话锁定同一个底层账号
- **智能语义分池**（Semantic Pool）：相同 System Prompt 的请求定向到专属账号池

---

## 六、多用户共用账号：上下文互相污染

多用户共用一个 API 账号时，缓存会被个性化信息破坏：

- 不同用户的 System Prompt 不同 → 前缀哈希不一致 → 缓存失效
- 用户 A 的对话上下文被用户 B 的请求覆盖 → KV Cache 被挤出
- 高频调用触发 RPM/TPM 速率限制 → 请求被拒绝

---

## 七、缓存分支模式：破坏的对立面

前六节讲的是什么**破坏**缓存。但缓存不只是被动消耗——合理的分支设计可以**主动利用**缓存。

### 7.1 Thread：用户驱动的持久分支

```
[main_1] [main_2] [main_3]
                   ├── [thread_A_1] [thread_A_2]
                   └── [thread_B_1] [thread_B_2]
```

父消息的 KV 状态是公共前缀。每条 thread 从分叉点独立续写，共享父节点缓存。切换 thread 时重新加载对应分支的 cache——代价是切换延迟，收益是分支内零重复计算。

与破坏模式的区别：thread 是**显式分支**（用户选择回复位置），破坏模式是**隐式污染**（工具/模型/用户无意中改变前缀）。

### 7.2 尾提示词：系统驱动的临时分支

```
KV cache（不变）：[历史对话]
旁路分支（新计算）：[尾提示词 + 用户问题]
  → turn 结束后，尾提示词丢弃，只保留结果
```

与 thread 的区别：尾提示词是一次性分支（单 turn），thread 是持久分支（跨 turn）。两者都利用前缀缓存，区别在生命周期。

### 7.3 压缩场景：原地替换

 Prefix Checkpoint 是尾提示词的特例——不是分支，是**原地替换**。不能改外部 Agent 循环，只能在 prompt 内部完成：LLM 执行工具调用后，旧历史被 checkpoint 替换，直接丢弃。不需要分支（连旁路分支都不需要），只需要同一次 turn 内完成替换。

→ 尾提示词的完整机制见 [缓存树和尾提示词优化](tail-prompt-optimization.md)

---

## 八、缓存有效 vs 无效的光谱

| 场景 | 前缀特征 | 缓存有效性 |
|------|---------|-----------|
| 单用户 / 单模型 / 纯文本对话 | 静态前缀 + 变化后缀 | **高** |
| 单用户 / 多轮对话（无工具） | 较稳定 | 中 |
| Thread 分支 | 显式分支，父节点共享 | **高**（分支内） |
| 尾提示词 | 临时分支，单 turn | **高**（前缀复用） |
| IDE 实时补全 | 持续变化 | **几乎为零** |
| 多模型切换 | 物理隔离 | **零** |
| 多工具混用 | 动态数据插入 | **极低** |
| 路由聚合 / 多用户共用 | 上下文互相覆盖 | **极低** |

**规律：参与方越多（模型、工具、用户、项目），前缀越不可控，缓存越无效。主动设计分支（thread、尾提示词）比被动污染好。**

---

## 九、本质结论

缓存有效 = 前缀稳定 = 上下文可预测。

现实世界的使用场景——IDE、多模型、多工具、多用户——恰恰是不可预测的。这不是 LLM 厂商能优化掉的问题，是结构性约束。

务实的应对：
- **不要指望缓存省钱**——把它当成意外之喜，不是成本模型的基础
- **高频短任务用本地小模型**——绕过缓存问题，直接省掉网络开销
- **长对话保持单模型、少工具**——如果你确实需要缓存，就减少变量
- **路由服务选粘性会话**——如果用聚合平台，确认它支持 Session Affinity

→ 相关认知见 [LLM 基础认知与心智模型](llm-fundamentals.md)

---

## 交叉引用

| 文档 | 关联论点 |
|------|---------|
| [LLM 基础认知与心智模型](llm-fundamentals.md) | 概率预测器 / 视野×速度 / 高维关键词匹配 / KV cache 树形结构 |
| [缓存树和尾提示词优化](tail-prompt-optimization.md) | 缓存分支机制、压缩场景原地替换 |
| [AI 编程批判](ai-programming-optimism-critique.md) | AI 编程的三重知识困境 |
| [编程语言选型](language-selection.md) | Python 作为 AI 生成最优语言 |
